-- Valid code examples for the API Validator
local part = Instance.new("Part")
part.Material = Enum.Material.Slate
part.Transparency = 0.5

local ui = Instance.new("SurfaceGui")
ui.Face = Enum.NormalId.Back
ui.LightInfluence = 0
